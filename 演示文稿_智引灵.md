# 智引灵 AI 导航导师

### 第三届"教育数字人大赛——我和数字人的故事"
### 学科应用组（智能体方向）

---

## 一、问题背景

---

### 真实痛点

> **"选课入口在哪？"**  
> **"作业在哪提交？"**  
> **"成绩怎么查？"**

- 教务系统入口深，新生需要 3-5 分钟才能找到目标功能
- MOOC、学习通、智慧树等平台功能布局各不相同
- 部分学生因数字素养不足而"望网兴叹"
- 教师面对不断更新的教学管理系统同样困惑

---

### 谁受影响最大？

| 群体 | 典型困境 |
|------|---------|
| 大一新生 | 不熟悉教务系统，开学选课焦虑 |
| 成人学习者 | 长期脱离学校，不习惯在线操作 |
| 老年学习者 | 数字鸿沟明显，需要手把手指引 |
| 留学生 | 中文界面不熟悉，找不到关键入口 |

---

## 二、解决方案

---

### 产品形态

**Chrome 浏览器扩展** —— 一个永远在线的 AI 导航伙伴

```
用户按住麦克风 → "我要选课"
       ↓
  数字人高亮选课按钮
       ↓
  语音播报："选课入口在导航栏第三个，已经帮你标记好了"
```

---

### 数字人形象：智引灵

> 虚拟助教 • 温暖耐心 • 陪伴式导航

- 4 种动画状态：idle → listening → thinking → speaking
- 双形态：侧边栏主形象 + 页面右下角浮窗 Widget
- 拖拽移动，不遮挡页面内容

---

## 三、核心功能演示

---

### 场景一：语音导航 × 教务系统

**用户**：*"成绩查询在哪？"*

→ 数字人自动定位"成绩查询"按钮，蓝色脉冲高亮

→ 语音："成绩查询在左侧菜单栏第二项"

---

### 场景二：文字输入 × 竞赛报名

**用户**：*输入"找比赛报名入口"*

→ 数字人扫描页面结构，高亮报名链接

→ 语音："报名入口在顶部导航'竞赛活动'下"

---

### 场景三：多步操作 × 课程选课

**用户**：*"帮我找到这学期的选修课列表"*

→ Step 1：高亮"教务系统"入口 → Step 2：点击"选课中心" → Step 3：自动滚动到选修课区域

---

### 场景四：页面描述 × 新生引导

**用户**：*"这个网站是做什么的？"*

→ 数字人综合页面标题、导航结构、内容摘要

→ 语音："这是一个MOOC学习平台，可以在这里选课、看视频、交作业"

---

### 场景五：降级兜底 × 容错保障

当一个选择器失效 → 自动尝试下一种方式（共5级）

- 精确CSS → 弱化选择器 → 文本搜索 → LLM重试 → 口头指引

> 永远不会"找不到就说抱歉"

---

## 四、技术架构

---

### 六层架构总览

```
展现层  ← SVG 数字人 + 浮窗 Widget（Shadow DOM 隔离）
  ↑
对话层  ← Whisper ASR 语音识别 + Chrome TTS 本地合成
  ↑
编排层  ← 状态机 + 5级选择器降级 + 任务队列 + 高亮验证
  ↑
大脑层  ← DeepSeek Chat / V4 Flash + 双模推理 + JSON 抢救
  ↑
预处理层 ← DOM 蒸馏（3000 → 300 token）+ 意图感知聚类
  ↑
感知层  ← Chrome Extension DOM Parser + SPA 路由检测
```

---

### 关键创新一：意图感知 DOM 蒸馏

| 用户说 | 系统优先扫描 |
|--------|------------|
| "登录/注册" | 顶部导航栏 → 页头 |
| "搜索/查找" | 搜索区域 → 导航栏 |
| "提交/保存" | 表单区 → 主内容区 |
| "设置/个人资料" | 侧边栏 → 导航栏 |
| "这是什么网站" | 均匀采集，LLM 综合 |

→ Token 压缩 90%，指令遵循率从 60% → 90%

---

### 关键创新二：高亮验证机制

**防止"说 A，指 B"**

```
LLM 输出：target=".nav-item-3" + verifyText="成绩查询"

前端执行：
① 高亮 .nav-item-3
② 检查该元素文字是否包含"成绩查询"
③ 不匹配 → 全页搜索真正含"成绩查询"的按钮 → 重新高亮 ✓
```

---

### 关键创新三：5 级选择器降级

| 级别 | 策略 | 命中率 |
|------|------|--------|
| L1 | 精确 CSS 选择器 | ~90% |
| L2 | 弱化选择器 + 文本筛选 | ~7% |
| L3 | XPath 文本匹配 | ~2% |
| L3b | 广域全元素文本搜索 | ~0.5% |
| L5 | 语音降级（口头指引） | 兜底 |

> 多级保障 = 不会因为找不到而失败

---

### 关键创新四：双模 LLM 推理

```
infer(fullPrompt) → 正常返回 ✓
       ↓ 安全过滤/token 超限
inferMinimal(仅区域摘要) → 自动降级重试
       ↓ JSON 被截断
recoverPartialJson() → 正则抢救已完成的字段
```

---

## 五、技术指标

---

| 指标 | 数值 |
|------|------|
| 端到端延迟 | < 3 秒 |
| DOM 蒸馏耗时 | < 50ms |
| 选择器首次命中率 | > 90% |
| 页面元素覆盖率 | 500 个可交互元素上限 |
| 代码规模 | 11 个模块，3041 行 |
| Extension 大小 | < 500KB |
| 支持的页面类型 | 所有网站（含 SPA） |

---

## 六、实施成效

---

### 用户测试数据

| 测试任务 | 无助手 | 有智引灵 | 提效 |
|----------|--------|---------|------|
| 找选课入口 | 45秒 | 8秒 | 82% |
| 找成绩查询 | 38秒 | 6秒 | 84% |
| 找报名链接 | 52秒 | 5秒 | 90% |
| 找提交作业按钮 | 35秒 | 4秒 | 89% |

- 用户满意度：4.6 / 5
- 测试人数：10 人
- 测试平台：教务系统、MOOC 平台、caet.org.cn

---

## 七、竞品对比

| 特性 | 智引灵 | 传统导航工具 |
|------|--------|------------|
| 数字人陪伴 | ✓ | ✗ |
| 语音交互 | ✓ | ✗ |
| 意图感知 | ✓ 10+场景 | ✗ |
| 降级保障 | 5级 | ✗ |
| 高亮验证 | ✓ | ✗ |
| 隐私保护 | 本地蒸馏 | 上传全DOM |
| 中文教育适配 | ✓ 优化 | 一般 |
| 开源 | ✓ | 部分 |

---

## 八、反思与展望

---

### 技术局限

- 依赖云端 API（断网不可用），规划 Ollama 离线降级
- 嘈杂环境语音准确率 70-80%，需前端降噪优化
- 仅桌面端 Chrome，移动端待拓展

### 伦理考量

- 本地蒸馏 → 不上传原始 DOM → 保护用户隐私
- 默认高亮 ≠ 自动点击 → 用户保留决策权
- 教师主导、技术辅助的边界意识

### 未来方向

- 移动端适配（覆盖最需要帮助的群体）
- 教育领域知识库 RAG（不只是导航，更是答疑）
- 多语言支持（服务留学生群体）

---

## 谢谢

**智引灵 AI 导航导师**

学科应用组 · 智能体方向

第三届教育数字人大赛 · 2026年5月

GitHub: https://github.com/a976xw7td/zhanghao-elite20
